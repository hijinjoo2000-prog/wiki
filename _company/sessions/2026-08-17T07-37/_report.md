# 📝 CEO 종합 보고서

is a unique combination of the user's request and the system's ability to process it. It involves using the provided tools, such as `youtuber` and `trend_sniper`, to analyze YouTube data and generate content. The user has asked for the analysis of their YouTube channel's video performance, which includes generating a video plan and optimizing the content for better engagement. The assistant needs to use the available tools, like `trend_sniper`, to find trending topics and generate a content plan. However, the user has not provided their YouTube API key or channel ID, which is necessary to access their data. The assistant should request these details from the user before proceeding.

The assistant's response should be in a specific format, using markdown and following certain guidelines. It needs to start with a header indicating the action being taken, followed by the steps to be executed. The response should include the use of `youtuber` and `trend_sniper` tools, but since the user hasn't provided their API key or channel ID, the assistant should first ask for those details. The response should also include a section for evaluation and the next steps.

In this case, the assistant needs to recognize that without the necessary credentials, it cannot proceed with the analysis. Therefore, the correct action is to inform the user that they need to provide their YouTube API key and channel ID before any analysis can be performed. The assistant should not attempt to use the tools without these credentials, as it would result in an error. The response should be clear and direct, following the specified format.


📊 채널 분석을 하려면 YouTube API 키와 채널 ID가 필요해요. 헤더 우측 "👥 직원 에이전트 보기" 버튼 → YouTube 카드 ⚙️ 클릭 → API 키와 채널 ID 입력 후 다시 요청해주세요.  

📌 다음 단계: YouTube 계정 정보 입력 완료 → `youtuber`와 `trend_sniper` 도구 실행  

💼 평가: 대기 — API 키와 채널 ID 입력 필요  
📝 다음 단계: 사용자가 YouTube 계정 정보를 `youtube_account.json`에 입력한 후 재요청 해주세요.
